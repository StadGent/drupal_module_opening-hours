{#
/**
 * @file
 * Template for the Opening Hours : OpenNow formatter.
 *
 * Available variables:
 * - type : The widget type to show.
 * - service_id : The opening hours Service ID.
 * - channel_id : The opening hours Channel ID.
 *
 * @see template_preprocess_vg_social_link()
 *
 * @ingroup themeable
 */
#}
<div class="opening-hours opening-hours--{{ type }}" data-service="{{ service_id }}" data-channel="{{ channel_id }}" data-type="{{ week }}">
  {{ service_id }} : {{ channel_id }}
</div>
